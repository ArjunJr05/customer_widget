name: salesiq_customer_widget
description: A Flutter package that automatically shows customer order data in Zoho SalesIQ widgets for better customer support.
version: 1.0.0
homepage: https://github.com/yourusername/salesiq_customer_widget

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  shared_preferences: ^2.2.2
  # salesiq_mobilisten: ^4.0.0  # Add this when SalesIQ package is available

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
